---
module: versiones/v0_0_0_4_sprint_2_5_auditoria/apendice_complementario_sprint_2_5
type: core
status: in_progress
created: '2025-05-26'
linked_to:
- metodologia_doc_ia_v2.md

---
# 🧩 Apéndice Complementario – Sprint 2.5 (Tareas Pendientes Detectadas por Auditoría)

Este apéndice detalla acciones adicionales no contempladas explícitamente en el checklist original, pero detectadas durante la auditoría del Sprint 2.5. Estas tareas no duplican elementos previos, sino que amplían la profundidad de la verificación con mejoras necesarias para la consolidación estructural de ALMA_RESIST.

---

## 🔁 Tareas complementarias sugeridas

1. **Normalización profunda del campo `module` en YAML**  
   Detectar y corregir rutas incompletas o inconsistentes dentro del campo `module` del frontmatter. Muchos archivos sólo declaran un subnivel (ej. `estructura_doc/plantilla_documento`) sin incluir el módulo raíz (`arquitectura/estructura_doc/...`).

2. **Revisión cruzada de campos `linked_to`**  
   Varios archivos YAML no incluyen el campo `linked_to`, o hacen referencia a rutas obsoletas. Debería implementarse validación semántica cruzada para alinear cada documento con su versión, bitácora o checklist correspondiente.

3. **Detección de archivos huérfanos fuera de módulos**  
   Se encontraron archivos en la raíz de `docs/` y en `ideas/` que no encajan en ningún módulo ni están registrados en `scripts_index.md` o README. Deberían reubicarse o archivarse si no cumplen con funciones activas.

4. **Autoetiquetado de documentos históricos**  
   Proponer una política para marcar automáticamente los documentos obsoletos o legacies con `status: archived` o similar, y separarlos del flujo operativo actual. Podría automatizarse en el futuro con un script de `curado_archivos.py`.

5. **Validación de consistencia semántica entre YAML y contenido real**  
   Varios documentos declaran en su frontmatter `type: core` pero no contienen contenido central, sino propuestas, pruebas o plantillas. Se recomienda implementar análisis IA para detectar esta inconsistencia y sugerir reclasificación.

6. **Control de duplicidad de scripts entre `scripts/` y `core/`**  
   Algunos scripts antiguos aún están presentes en `core/` o `commands/` pese a haber sido migrados a `docs/utilidades/scripts/`. Se recomienda forzar verificación de duplicados por hash y eliminar los residuales.

7. **Versión snapshot Sprint 2.5 faltante en `docs/versiones/`**  
   El snapshot correspondiente a este sprint aún no se encuentra registrado formalmente. Se sugiere generar la carpeta `v0.0.0.5_snapshot_sprint_2.5` y documentar su estructura resultante.

---

## ✅ Siguientes pasos sugeridos

- Implementar un script `detect_inconsistencias_yaml.py` para validar campos `module`, `type`, `status` versus su contenido y ubicación real.
- Establecer convención para rutas completas en `module`, incluyendo prefijo de módulo raíz.
- Generar y versionar snapshot de cierre del sprint con todos los cambios aplicados.
- Crear script de curaduría automática de documentos obsoletos (`status: archived`).

---

Este apéndice complementa el checklist de Sprint 2.5 como insumo técnico para el Sprint 2.6.
